name: Validate distributions

on:
  workflow_call:
    inputs:
      artifact-prefix:
        description: Prefix used by distribution build artifacts.
        required: true
        type: string

jobs:
  install-all:
    name: opake[all] (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.13"]
    steps:
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          enable-cache: false

      - name: Download distribution artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: ${{ inputs.artifact-prefix }}-*
          path: dist/
          merge-multiple: true

      - name: Install opake[all] from built wheels
        run: |
          opake_wheels=(dist/opake-*.whl)
          if [[ "${#opake_wheels[@]}" -ne 1 ]]; then
            echo "::error::Expected one opake wheel, found ${#opake_wheels[@]}"
            exit 1
          fi
          uv venv --python ${{ matrix.python-version }} .artifact-venv
          uv pip install \
            --python .artifact-venv/bin/python \
            --find-links dist \
            --only-binary :all: \
            "${opake_wheels[0]}[all]"

      - name: Run cross-stack scenario
        run: |
          cd "$RUNNER_TEMP"
          "$GITHUB_WORKSPACE/.artifact-venv/bin/python" - <<'PY'
          import math
          from importlib.metadata import distributions, version

          import opake.dpftrl.accounting as dpftrl
          import opake.dpsgd.accounting as dpsgd
          from opake.dpftrl.noise import identity_strategy

          umbrella_version = version("opake")
          opake_versions = {
              dist.metadata["Name"]: dist.version
              for dist in distributions()
              if (name := dist.metadata["Name"]) == "opake"
              or name.startswith("opake-")
          }
          assert len(opake_versions) > 1
          assert set(opake_versions.values()) == {umbrella_version}, opake_versions

          dpsgd_process = dpsgd.gaussian(1.0)
          dpftrl_process = dpftrl.poisson(
              dpftrl.mf_gaussian(1.0, identity_strategy()),
              sample_rate=0.01,
              n_steps=4,
          )
          epsilon = (dpsgd_process | dpftrl_process).epsilon_at(1e-5)
          assert math.isfinite(epsilon) and epsilon > 0
          print(f"opake[all] cross-stack epsilon: {epsilon:.6f}")
          PY
