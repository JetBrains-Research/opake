[build-system]
requires = ["setuptools>=68.0", "setuptools-scm>=8", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "opake-alignment"
dynamic = ["version"]
description = "Functional primitives for DP-safe preference learning (DPO, SFT)"
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [{ name = "Opake Contributors" }]
requires-python = ">=3.11,<3.14"
keywords = [
    "differential-privacy",
    "preference-learning",
    "dpo",
    "pytorch",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Security",
]
dependencies = [
    "torch>=2.9.0",
    "transformers>=5.0.0,<6",
    "datasets>=2.0.0",
    "numpy>=1.24",
    "peft>=0.13.0",
    # safetensors is the on-disk format for the DPO reference-logp cache
    # (`opake.api.alignment.dpo.reference._precompute`). Already pulled in
    # transitively by transformers; declared directly because we import it.
    "safetensors>=0.4",
    "opake-engine>=0.0.0.dev0",
    "opake-base>=0.0.0.dev0",
    # NO opake-patches: alignment-specific kernels live inside this package
    #   (Opake_FusedLinearPreference is self-contained pure-PyTorch).
    # NO opake-dpsgd, NO opake-dpftrl, NO opake-optimizers
    #   — mechanism + optimizer are chosen by the caller.
]

[project.optional-dependencies]
# Recipe path for SFT users who want opake_linear_cross_entropy_loss memory
# optimization. Not needed for the alignment primitives themselves.
patches = ["opake-patches>=0.0.0.dev0"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["opake.alignment*", "opake.api.alignment*"]
namespaces = true

[tool.setuptools.package-data]
"*" = ["py.typed"]

[tool.setuptools_scm]
root = "../.."
fallback_version = "0.0.0"
git_describe_command = "git describe --dirty --tags --long --match v*"

[tool.uv]
package = true

[tool.uv.sources]
opake-engine = { workspace = true }
opake-base = { workspace = true }
opake-patches = { workspace = true }
